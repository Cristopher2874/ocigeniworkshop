This folder is currently work in progress on how to integrate skills

Consider that the right way of doing the skills with agents is using shell tool

Shell tool is not currently supported by the oci/openai endpoint non native

So there are other techniques demonstrated here.

Humanizer skill is download

Others where generated as example

use them as temp while the shell and tool_search are enabled into OCI features.